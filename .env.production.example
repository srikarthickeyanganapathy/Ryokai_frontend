# Copy to .env.production for local production builds,
# OR set these directly as Environment Variables in Vercel.

# Public HTTPS URL of your deployed backend (must match app.oauth.backend-url on the server).
VITE_API_URL=https://api.yourdomain.com/api/v1
